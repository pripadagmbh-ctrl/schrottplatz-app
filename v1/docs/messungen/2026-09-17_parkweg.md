# Der Weg zum Warteplatz — vorher und nachher, 17.09.2026

**Kein Gerätetest.** Alles hier ist kopflos gemessen (`vite-node`, echte
Rapier-Welt, echter Fuhrpark, echte Behälter). Der Anlass ist Patricks
Gerätebefund vom 17.09.2026; die Nachprüfung am iPad steht aus.

| | |
|---|---|
| Anlass | **„LKWS fahren durch Müllcontainer. Objekte fahren durch einander hindurch"** |
| Diagnose | E-097, `docs/messungen/2026-09-17_durchfahrt.md` |
| Reparatur | E-098 |
| Werkzeuge | `tools/durchfahrt.ts` (Bericht), `tools/parkfahrten.ts` (zehn Fuhren), `tools/parkweg-probe.ts` (Abzweigpunkte), `tools/absturz-probe.ts` (Rapier-Verhalten) |
| Rohausgaben | `2026-09-17_durchfahrt.txt` (vorher), `2026-09-17_durchfahrt-nachher.txt`, `2026-09-17_parkfahrten.txt` |
| Nullprobe | beide Läufe bestanden — 0,00 m neben dem Hof, 2,80 m auf dem Kaffeewagen |

## Die Durchdringungstabelle

| Paar | vorher | nachher |
|---|---|---|
| LKW × MÜLL-Container, Phase `toPark` | **3,10 m** (volle Fahrzeugbreite), Bild 2259 | **0,00 m** |
| dasselbe mit Container auf seinem **Startplatz** | **3,10 m** | **0,00 m** |
| Versatz des Containers durch die Durchfahrt | 0,018 · 0,040 · 0,080 · 0,092 m | **0,000 m** in allen 13 Proben |
| LKW × liegender Schrott (400 kg auf 1,0 \| −18,4) | **1,22 m verschoben** | **0,00 m** |
| LKW × Ostwand, Kehre am Abladeplatz, über alle 360 Gierlagen | 0,60 m | **0,60 m — unverändert, nicht meine Entscheidung** |
| LKW × Ostwand, tatsächlich gefahren | 0,27 m | 0,00 bis 0,27 m, je nach Lauf |
| Behälter × Westmauer (die Gegenprobe) | 0,01 m — gehalten | 0,01 m — gehalten |

## Das Gespenst

Abgeleitetes Tempo des LKW-Rahmens. Rapier rechnet es aus *nächste Pose −
jetzige Pose*; null heißt **versetzt statt bewegt**, und dann sieht der Löser
keinen Stoß, sondern eine ruhende Überdeckung.

| Phase | vorher | nachher |
|---|---|---|
| `approach` (Vergleichsmaß) | 4,891 | 4,890 |
| **`toPark`** | **0,171** | **4,890** |
| **`parkRueck`** | **0,026** | **2,043** |
| `ausfaedeln` | — | 0,000 (ein einziges Bild, E-094, unberührt) |
| `out` | 79,200 (Sprung, seit E-094 weg) | 4,890 |

## Zehn Fuhren in Folge

`npx vite-node tools/parkfahrten.ts`, drei Warteplätze im Wechsel, Container auf
Patricks Stelle (−2,80 | −15,40):

* **10 von 10 angekommen**, jede in Phase `parked`
* Restabstand zum Warteplatz je 0,00 m, Gierlage je 180,0 Grad (Heck zur Wand)
* tiefste Durchdringung über alle 40.345 Bilder: **0,00 m**
* 119 Körper, Spitze 93 wache dynamische, **1,497 ms Physik je Bild**
  (kopflos, mit zehn abgekippten Fuhren auf dem Hof und ohne Bagger, der sie
  wegräumt — das ist die obere Grenze, nicht der Normalfall)

## Der harte Absturz

`tools/absturz-probe.ts`, dieselbe Rapier-Fassung wie im Spiel:

| Frage an einen entfernten Körper | Antwort |
|---|---|
| `isValid()` | `false` — gutartig |
| `handle` | **2,1e−314 statt 0** — Unsinn, aber ohne Fehler |
| `isDynamic()` | **`RuntimeError: unreachable`** |
| danach `world.step()` | **„recursive use of an object detected which would lead to unsafe aliasing in rust"** |
| eine **neue** Welt | läuft wieder |

Die vierte Zeile ist der Grund, warum das vor allem anderen drankam: Es steht
nicht ein Teil still, sondern das Spiel.

## Was nicht repariert ist

1. **0,60 m Ostwand beim Eindrehen** — kein Rechenfehler, sondern zu wenig
   Platz (Abladeplatz 2,15 m vor der Wand, Umriss überstreicht 4,0 m
   Halbmesser). Unverändert. Gehört Patrick.
2. **Der gemeldete Containerumriss** ist 7 cm schmaler und 17,5 cm flacher als
   der gebaute (`containers.ts`, `get hindernis`). Unverändert.
3. **`CHASSIS_PAD` 1,30 m gegen `UNTERWAGEN_R` 2,60 m** am Bagger —
   `src/excavator/` ist nicht angefasst worden.
